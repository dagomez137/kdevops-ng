# Help: Built-in overrides for virtio-fs
# VIRTIO_FS depends on FUSE_FS. VIRTIO is tristate (=m from
# VIRTIO_PCI=m in core). Force the full chain to =y.
CONFIG_FUSE_FS=y
CONFIG_VIRTIO_FS=y
CONFIG_VIRTIO=y
CONFIG_VIRTIO_PCI=y
