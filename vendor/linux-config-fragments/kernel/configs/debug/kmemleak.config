# Help: Kernel memory leak detector
#
# Depends on DEBUG_KERNEL (from core.config via DEBUG_FS).
# See: https://docs.kernel.org/dev-tools/kmemleak.html
CONFIG_DEBUG_KMEMLEAK=y
CONFIG_SAMPLES=y
CONFIG_SAMPLE_KMEMLEAK=m
