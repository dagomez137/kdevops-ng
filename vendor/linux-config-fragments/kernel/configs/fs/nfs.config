# Help: NFS client and server
#
# NFS_V4_2 depends on NFS_V4_1 which depends on NFS_V4, so the full
# chain has to be set for NFSv4.2 to actually become reachable at
# merge_config time.
CONFIG_NFS_FS=m
CONFIG_NFS_V4=m
CONFIG_NFS_V4_1=y
CONFIG_NFS_V4_2=y
CONFIG_NFSD=m
