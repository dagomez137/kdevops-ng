# Help: KUnit with every test built in
#
# The =y mirror of test/kunit.config: every test the enabled
# subsystems allow is compiled into the kernel instead of built as a
# module, for a module-free kernel. Self-contained: inlines the
# framework the tests depend on.
CONFIG_KUNIT=y
CONFIG_KUNIT_ALL_TESTS=y
